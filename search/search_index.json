{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"OpenBSH: Bluetooth Shell","text":"<p>Welcome to the official documentation for OpenBSH \u2014 a robust, cross-platform Bluetooth Shell service. </p> <p>OpenBSH provides secure, SSH-style remote shell access over Bluetooth RFCOMM, enabling powerful out-of-band management for systems where network access is unavailable, restricted, or compromised.</p>"},{"location":"#why-openbsh","title":"Why OpenBSH?","text":"<p>In many secure or edge environments, traditional IP networks (Wi-Fi, Ethernet) might be deliberately air-gapped, or a misconfiguration might lock administrators out. OpenBSH leverages Bluetooth to provide a reliable, proximity-based management channel.</p>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>Cross-Platform Support: Fully functional server implementations for both Windows (as a Windows Service) and Linux (as a Systemd Daemon), along with cross-platform Python clients.</li> <li>Enterprise-Grade Security: All post-authentication traffic is encrypted using AES-256-GCM with a fresh random session key per connection. Standalone passwords are stored with PBKDF2-HMAC-SHA256.</li> <li>SSH-Style PTY Experience: Enjoy an interactive shell experience with pseudo-terminal (PTY) emulation, supporting standard shell utilities, command history, and continuous output.</li> <li>Native OS Integration: <ul> <li>Windows: Integrates with <code>win32serviceutil</code>, uses <code>CreateProcessAsUser</code> for shell impersonation, and authenticates with <code>LogonUserW</code>.</li> <li>Linux: Integrates with Systemd, uses <code>pty.openpty</code> for terminal emulation, and supports PAM authentication with <code>/etc/shadow</code> fallback.</li> </ul> </li> </ul>"},{"location":"#documentation-structure","title":"Documentation Structure","text":"<p>This site is structured to help you understand, deploy, and manage OpenBSH:</p> <ul> <li>Architecture: Deep dive into how OpenBSH works under the hood.</li> <li>Setup Guides: Step-by-step instructions for deploying OpenBSH on Linux, Windows, and configuring Clients.</li> <li>Usage Guide: Learn how to connect, manage users, and troubleshoot common issues.</li> <li>Protocol &amp; Security: A detailed look at the custom BSH Wire Protocol and the cryptographic implementation.</li> </ul>"},{"location":"architecture/","title":"System Architecture","text":"<p>OpenBSH is designed with a robust, cross-platform architecture that cleanly separates the core wire protocol and cryptography from platform-specific system integration (like pseudo-terminals and service management).</p>"},{"location":"architecture/#high-level-component-view","title":"High-Level Component View","text":"<p>At its core, OpenBSH consists of a Client, a Server, and a shared Protocol Layer.</p> <pre><code>graph TD\n    subgraph Client System\n        C[BSH Client]\n        CryptoC[bsh_crypto.py]\n        ProtoC[bsh_protocol.py]\n        C --- CryptoC\n        C --- ProtoC\n    end\n\n    subgraph RFCOMM Bluetooth Link\n        BT((Bluetooth RFCOMM\\nPost-auth AES-256-GCM))\n    end\n\n    subgraph Server System\n        S[BSH Server Daemon/Service]\n        CryptoS[bsh_crypto.py]\n        ProtoS[bsh_protocol.py]\n        Auth[Auth Module: PAM / Win32]\n        PTY[Shell / PTY Emulation]\n\n        S --- CryptoS\n        S --- ProtoS\n        S --- Auth\n        S --- PTY\n    end\n\n    C &lt;==&gt; BT\n    BT &lt;==&gt; S</code></pre>"},{"location":"architecture/#core-modules","title":"Core Modules","text":"<p>OpenBSH relies on several key Python modules. To ensure maximum compatibility between Windows and Linux, the cryptographic and protocol logic is completely identical across platforms.</p>"},{"location":"architecture/#1-bsh_protocolpy-the-wire-protocol","title":"1. <code>bsh_protocol.py</code> (The Wire Protocol)","text":"<p>This file defines the strict packet structure used to communicate over Bluetooth. Because Bluetooth RFCOMM provides a reliable stream (similar to TCP), <code>bsh_protocol.py</code> handles framing: defining the Start of Frame (<code>0xAA</code>), message types (e.g., <code>MSG_HELLO</code>, <code>MSG_DATA_IN</code>), payloads, and checksum validation.</p>"},{"location":"architecture/#2-bsh_cryptopy-the-security-layer","title":"2. <code>bsh_crypto.py</code> (The Security Layer)","text":"<p>Once the server and client mutually authenticate, <code>bsh_crypto.py</code> is engaged. It implements AES-256-GCM encryption. All subsequent shell traffic is encrypted and authenticated. The GCM (Galois/Counter Mode) tag ensures that any tampering with the packet over the air is instantly detected and the connection is dropped.</p>"},{"location":"architecture/#3-platform-specific-server-logic","title":"3. Platform-Specific Server Logic","text":"<p>While the protocol is identical, interacting with the operating system requires highly tailored code.</p>"},{"location":"architecture/#windows-service-bsh_server_servicepy-bsh_servicepy","title":"Windows Service (<code>bsh_server_service.py</code> &amp; <code>bsh_service.py</code>)","text":"<ul> <li>Service Management: Uses <code>win32serviceutil</code> to run seamlessly in the background as a native Windows service.</li> <li>Authentication: Uses Windows <code>LogonUserW</code> (via <code>ctypes</code>) to validate credentials for the target Windows account.</li> <li>Impersonation: Uses <code>CreateProcessAsUser</code> to spawn <code>cmd.exe</code> or <code>powershell.exe</code> securely under the authenticated user's context.</li> </ul>"},{"location":"architecture/#linux-daemon-bsh_server_servicepy-bsh_servicepy","title":"Linux Daemon (<code>bsh_server_service.py</code> &amp; <code>bsh_service.py</code>)","text":"<ul> <li>Service Management: Wrapped in a native <code>systemd</code> unit (<code>bsh.service</code>).</li> <li>Authentication: Uses the <code>python-pam</code> library to authenticate against PAM (Pluggable Authentication Modules). If PAM fails or isn't present, it falls back to parsing <code>/etc/shadow</code>.</li> <li>Terminal Emulation: Uses <code>pty.openpty()</code> to create a proper pseudo-terminal, then calls <code>os.fork()</code> and <code>os.execv()</code> (along with <code>setuid</code>/<code>setgid</code>) to drop root privileges and run the user's default shell (e.g., <code>/bin/bash</code>).</li> </ul>"},{"location":"architecture/#authentication-mechanism","title":"Authentication Mechanism","text":"<p>OpenBSH performs a two-step password authentication exchange followed by session key establishment.</p> <pre><code>sequenceDiagram\n    participant C as Client\n    participant S as Server\n\n    C-&gt;&gt;S: MSG_HELLO (Client Info)\n    S-&gt;&gt;C: MSG_HELLO (Server Info, Auth Methods)\n\n    C-&gt;&gt;S: MSG_AUTH_PASSWORD_REQUEST (Username)\n    S-&gt;&gt;C: MSG_AUTH_PASSWORD_CHALLENGE (32-byte Challenge)\n    C-&gt;&gt;S: MSG_AUTH_PASSWORD_RESPONSE (JSON payload with password)\n\n    Note over S: Server verifies the password against&lt;br/&gt;the BSH password DB or native OS auth\n\n    S-&gt;&gt;C: MSG_AUTH_SUCCESS (Session Key)\n\n    Note over C, S: Connection switches to AES-256-GCM\n    C-&gt;&gt;S: MSG_DATA_IN (Encrypted)\n    S-&gt;&gt;C: MSG_DATA_OUT (Encrypted)</code></pre> <ol> <li>Hello Exchange: Both sides advertise their version and OS capabilities. This is critical for clients to adjust their terminal emulation (e.g., disabling local echo if the server is Linux).</li> <li>Password Exchange: The server sends a random challenge, then expects <code>MSG_AUTH_PASSWORD_RESPONSE</code>. In the current client implementation, that response carries the plaintext password inside the BSH packet payload.</li> <li>Session Key Negotiation: If authentication succeeds, the server generates a random AES-256 session key and sends it to the client. From this moment, subsequent packet payloads are encrypted.</li> </ol>"},{"location":"protocol-security/","title":"Protocol &amp; Security","text":"<p>OpenBSH relies on a highly specialized, custom Wire Protocol built directly over Bluetooth RFCOMM. This design ensures that communications are resilient to Bluetooth frame fragmentation, and that packet payloads are encrypted after authentication succeeds.</p>"},{"location":"protocol-security/#cryptography-design","title":"Cryptography Design","text":"<p>All BSH traffic is secured using AES-256-GCM (Galois/Counter Mode). This provides both confidentiality (encryption) and authenticity (tamper-proofing).</p>"},{"location":"protocol-security/#key-derivation-storage","title":"Key Derivation &amp; Storage","text":"<ul> <li>Standalone Password Database: When using the <code>bsh_password.py</code> database, passwords are never stored in plain text. They are hashed using PBKDF2-HMAC-SHA256 with a randomly generated salt and <code>100,000</code> iterations.</li> <li>Session Keys: The encryption keys used for the data stream are ephemeral. A new 32-byte (256-bit) AES session key is generated securely by the server (<code>os.urandom</code>) upon every successful authentication.</li> </ul>"},{"location":"protocol-security/#wire-encryption-format","title":"Wire Encryption Format","text":"<p>Once the session key is negotiated via <code>MSG_AUTH_SUCCESS</code>, both the client and server transition to encrypted mode. Every subsequent packet payload is replaced with an AES-GCM envelope.</p> <pre><code>| IV (12 bytes) | AES-GCM Ciphertext (Variable) | Auth Tag (16 bytes) |\n</code></pre> <ul> <li>IV (Initialization Vector): A unique 12-byte nonce generated for every single packet using <code>os.urandom</code>. This prevents replay attacks and ensures encryption uniqueness even if the identical payload is sent twice.</li> <li>Ciphertext: The fully encrypted original payload.</li> <li>Auth Tag: The 16-byte GCM authentication tag. The receiving side will immediately close the socket if this tag does not perfectly validate the ciphertext against the Session Key.</li> </ul>"},{"location":"protocol-security/#wire-protocol-bsh_protocolpy","title":"Wire Protocol (<code>bsh_protocol.py</code>)","text":"<p>Because Bluetooth RFCOMM is a continuous stream, OpenBSH implements a custom framing protocol to define packet boundaries.</p>"},{"location":"protocol-security/#packet-structure","title":"Packet Structure","text":"<p>Every OpenBSH packet adheres to a strict binary format:</p> <pre><code>packet-beta\n0-7: \"SOF (0xAA)\"\n8-23: \"Length (16-bit, big-endian)\"\n24-31: \"Type\"\n32-39: \"Payload byte 0\"\n40-47: \"Payload byte 1\"\n48-55: \"...\"\n56-63: \"Payload byte N-1\"\n64-71: \"Checksum (XOR)\"</code></pre> <p>For a payload of length <code>N</code>, the on-wire byte order is:</p> <pre><code>SOF | LEN_H | LEN_L | TYPE | PAYLOAD[0..N-1] | CHECKSUM\n</code></pre> <ol> <li>SOF (Start of Frame): A fixed byte (<code>0xAA</code>). If the receiver loses sync, it reads byte-by-byte until it finds <code>0xAA</code>.</li> <li>Length: A 16-bit unsigned integer defining exactly how many bytes the payload contains.</li> <li>Message Type: An 8-bit integer defining the purpose of the packet.</li> <li>Payload: The actual data carried by the packet.</li> <li>Checksum: A single-byte XOR checksum over <code>Length + Type + Payload</code>. The SOF byte is intentionally excluded.</li> </ol>"},{"location":"protocol-security/#message-types","title":"Message Types","text":"<p>The protocol defines the following core message types:</p> Enum Name Description <code>0x01</code> <code>MSG_HELLO</code> Initial capability and OS exchange. <code>0x02</code> <code>MSG_DISCONNECT</code> Clean disconnect request. <code>0x03</code> <code>MSG_KEEPALIVE</code> Keepalive packet. <code>0x07</code> <code>MSG_AUTH_SUCCESS</code> Server sends authentication success and the AES session key. <code>0x08</code> <code>MSG_AUTH_FAILURE</code> Server sends an authentication or protocol error. <code>0x09</code> <code>MSG_AUTH_PASSWORD_REQUEST</code> Client begins password authentication. <code>0x0A</code> <code>MSG_AUTH_PASSWORD_CHALLENGE</code> Server sends a random challenge. <code>0x0B</code> <code>MSG_AUTH_PASSWORD_RESPONSE</code> Client sends the password-auth response payload. <code>0x10</code> <code>MSG_DATA_IN</code> Client sends shell input. <code>0x11</code> <code>MSG_DATA_OUT</code> Server sends shell stdout. <code>0x12</code> <code>MSG_DATA_ERR</code> Server sends shell stderr. <code>0x20</code> <code>MSG_INTERRUPT</code> Client requests a shell interrupt. <code>0x21</code> <code>MSG_WINDOW_SIZE</code> Client sends terminal size information."},{"location":"protocol-security/#the-authentication-flow","title":"The Authentication Flow","text":"<p>The authentication flow is a critical component of OpenBSH. The current implementation performs a challenge step and then sends a password response payload that the server verifies against the BSH password database or the native OS authentication path.</p> <pre><code>sequenceDiagram\n    participant C as Client\n    participant S as Server\n\n    C-&gt;&gt;S: [MSG_HELLO] {\"os\": \"Linux\", \"features\": [\"password\"]}\n    S-&gt;&gt;C: [MSG_HELLO] {\"os\": \"Windows\", \"features\": [\"password\"]}\n\n    C-&gt;&gt;S: [MSG_AUTH_PASSWORD_REQUEST] {\"username\": \"admin\"}\n\n    Note over S: Server generates 32-byte Nonce\n    S-&gt;&gt;C: [MSG_AUTH_PASSWORD_CHALLENGE] {\"challenge\": \"&lt;hex&gt;\"}\n    C-&gt;&gt;S: [MSG_AUTH_PASSWORD_RESPONSE] {\"password\": \"&lt;plaintext&gt;\"}\n\n    Note over S: Server verifies the password against the BSH DB&lt;br/&gt;or native OS auth, then generates a 32-byte AES session key.\n\n    S-&gt;&gt;C: [MSG_AUTH_SUCCESS] {\"session_key\": \"&lt;hex&gt;\"}\n\n    Note over C, S: Both sides transition to AES-256-GCM\n    C-&gt;&gt;S: [MSG_DATA_IN] (Encrypted Payload)</code></pre>"},{"location":"protocol-security/#os-authentication-fallback","title":"OS Authentication Fallback","text":"<p>If the user does not exist in the standalone database, the server falls back to native OS authentication (PAM on Linux, <code>LogonUserW</code> on Windows).</p> <p>Important: the current client/server implementation does not perform an extra Diffie-Hellman or RSA exchange before OS authentication. The password is sent in the <code>MSG_AUTH_PASSWORD_RESPONSE</code> payload before the AES session key becomes active. The surrounding Bluetooth pairing and transport behavior therefore matter to the threat model.</p>"},{"location":"usage/","title":"Usage Guide","text":"<p>Once OpenBSH is installed and running on a target server, managing users and connecting via the client is straightforward.</p>"},{"location":"usage/#managing-the-service","title":"Managing the Service","text":"<p>The OpenBSH daemon runs in the background. Depending on your operating system, you use different tools to verify its status.</p> <p>=== \"Linux (Systemd)\"     <pre><code># View the status of the service\nsystemctl status bsh\n\n# Follow the logs in real-time\njournalctl -u bsh -f\n</code></pre></p> <p>=== \"Windows (Service MMC)\"     <pre><code># Check if the service is running\nGet-Service BSHService\n\n# Check logs\nGet-Content \"C:\\ProgramData\\BSH\\logs\\bsh_service.log\" -Wait\n</code></pre></p>"},{"location":"usage/#authentication-modes","title":"Authentication Modes","text":"<p>OpenBSH supports two primary modes of authentication:</p> <ol> <li> <p>Native OS Authentication (Default):</p> <ul> <li>On Windows, OpenBSH uses the <code>LogonUserW</code> API to verify the target Windows account password.</li> <li>On Linux, OpenBSH uses PAM when available and falls back to <code>/etc/shadow</code>-based verification when running with sufficient privileges.</li> </ul> </li> <li> <p>Standalone BSH Database:</p> <ul> <li>OpenBSH ships with its own highly secure, standalone password database (<code>bsh_password.py</code>). </li> <li>This is useful if you want to grant BSH access using a different password than the system password, or if you want to strictly control which users can access the system via Bluetooth.</li> <li>Standalone users must map to a valid OS user account, because OpenBSH needs a valid OS context to impersonate and spawn the shell.</li> </ul> </li> </ol>"},{"location":"usage/#managing-standalone-users","title":"Managing Standalone Users","text":"<p>If you choose to use the Standalone Database, you manage users via the <code>bsh_password.py</code> utility.</p> <p>[!NOTE] On Linux, the utility is located at <code>/opt/bsh/bsh_password.py</code> and requires <code>sudo</code>. On Windows, it is located in your installation directory and requires an Administrator prompt.</p>"},{"location":"usage/#add-a-new-user","title":"Add a New User","text":"<p><pre><code>python bsh_password.py adduser johndoe\n</code></pre> This assumes that the OS account \"johndoe\" already exists.</p>"},{"location":"usage/#map-to-a-different-os-user","title":"Map to a Different OS User","text":"<p>If you want the BSH username to be different from the OS username: <pre><code>python bsh_password.py adduser bsh_admin --system-user root\n</code></pre> When <code>bsh_admin</code> logs in, they will be dropped into a <code>root</code> shell.</p>"},{"location":"usage/#change-a-password","title":"Change a Password","text":"<pre><code>python bsh_password.py passwd johndoe\n</code></pre>"},{"location":"usage/#list-users","title":"List Users","text":"<pre><code>python bsh_password.py list\n</code></pre>"},{"location":"usage/#delete-a-user","title":"Delete a User","text":"<pre><code>python bsh_password.py deluser johndoe\n</code></pre>"},{"location":"usage/#connecting-via-the-client","title":"Connecting via the Client","text":"<p>The OpenBSH client provides an interactive, terminal-like experience.</p>"},{"location":"usage/#basic-connection","title":"Basic Connection","text":"<p>To connect, pass the username and Bluetooth MAC address of the target server to your OS-specific client.</p> <p>From Windows: <pre><code>python bsh_client_windows.py johndoe@00:11:22:33:44:55\n</code></pre></p> <p>From Linux: <pre><code>python3 bsh_client_linux.py johndoe@00:11:22:33:44:55\n</code></pre></p>"},{"location":"usage/#custom-channels","title":"Custom Channels","text":"<p>Linux defaults to channel <code>1</code>, but the active RFCOMM channel may differ if you reconfigure it or if the platform assigns a different bound channel. If needed, specify the channel explicitly during connection:</p> <p>From Windows: <pre><code>python bsh_client_windows.py johndoe@00:11:22:33:44:55 -p 4\n</code></pre></p> <p>From Linux: <pre><code>python3 bsh_client_linux.py johndoe@00:11:22:33:44:55 -p 4\n</code></pre></p>"},{"location":"usage/#interactive-shell-experience","title":"Interactive Shell Experience","text":"<p>Once connected and authenticated, you are dropped into a native shell (<code>cmd.exe</code>/<code>powershell.exe</code> on Windows, or <code>/bin/bash</code> on Linux).</p> <p>The client will automatically handle: - Terminal Sizing: When you resize your client terminal window, a <code>MSG_WINDOW_SIZE</code> packet is sent to the server to seamlessly resize the remote PTY. - Echo Suppression: If connected to a Linux server, the client will suppress local keyboard echo, relying purely on the server's PTY, enabling complex terminal applications like <code>vim</code> or <code>htop</code>. - Keepalives: The client sends background keepalive pings every 0.5s to ensure the Bluetooth link remains active and doesn't timeout during periods of inactivity.</p> <p>To exit the session, simply type <code>exit</code> in the shell or press <code>Ctrl+C</code>.</p>"},{"location":"setup/client/","title":"Client Setup","text":"<p>The OpenBSH Client is a Python-based utility that allows you to connect to an OpenBSH server over Bluetooth RFCOMM. </p> <p>Because the heavy lifting (PTY emulation, service management, user impersonation) is handled entirely by the server, the client is relatively lightweight and highly cross-platform.</p>"},{"location":"setup/client/#prerequisites","title":"Prerequisites","text":"<p>The client requires a Python 3 environment and a Bluetooth adapter. It runs natively on both Windows and Linux.</p>"},{"location":"setup/client/#install-dependencies","title":"Install Dependencies","text":"<p>Whether you are on Windows or Linux, install the <code>cryptography</code> package via <code>pip</code>. On Linux, the client uses the Python standard library Bluetooth socket support for RFCOMM connections. <code>PyBluez</code> is optional and only helps with SDP-based channel discovery.</p> <p>=== \"Windows\"     <pre><code>pip install cryptography\n</code></pre></p> <p>=== \"Linux\"     <pre><code>sudo apt install libbluetooth-dev\npip3 install cryptography\n</code></pre></p>"},{"location":"setup/client/#installation","title":"Installation","text":"<p>There is no formal installation process for the client. The client is a standalone set of Python files designed to be run portably.</p> <ol> <li>Clone or download the OpenBSH repository.</li> <li>Navigate to the <code>Client/</code> directory.</li> </ol>"},{"location":"setup/client/#project-structure","title":"Project Structure","text":"<pre><code>Client/\n\u251c\u2500\u2500 bsh_client.py           # Shared client implementation\n\u251c\u2500\u2500 bsh_client_windows.py   # Windows-specific client logic\n\u251c\u2500\u2500 bsh_client_linux.py     # Linux-specific client logic\n\u251c\u2500\u2500 bsh_protocol.py         # The shared wire protocol\n\u2514\u2500\u2500 bsh_crypto.py           # The shared cryptography library\n</code></pre>"},{"location":"setup/client/#basic-usage","title":"Basic Usage","text":"<p>To connect to a server, you only need the target username and the Bluetooth MAC Address of the target machine. Ensure you are running the specific client script for your Operating System: <code>bsh_client_windows.py</code> for Windows, or <code>bsh_client_linux.py</code> for Linux.</p>"},{"location":"setup/client/#finding-the-server-mac-address","title":"Finding the Server MAC Address","text":"<p>If you don't know the MAC address of the target server, pairing the devices through the OS Bluetooth settings is usually the most reliable path. Command-line tools can also help, depending on platform support.</p> <p>On the Server (Linux): <pre><code>hciconfig -a\n# Look for \"BD Address: XX:XX:XX:XX:XX:XX\"\n</code></pre></p> <p>On the Server (Windows): <pre><code>Get-PnpDevice -Class Bluetooth\n# Then inspect the adapter in Device Manager or Windows Bluetooth settings\n</code></pre></p>"},{"location":"setup/client/#connecting-to-the-server","title":"Connecting to the Server","text":"<p>Once you have the MAC address, use the OS-specific client script to connect:</p> <p>From Windows: <pre><code>python bsh_client_windows.py user@00:11:22:33:44:55\n</code></pre></p> <p>From Linux: <pre><code>python3 bsh_client_linux.py user@00:11:22:33:44:55\n</code></pre></p> <p>If the server is running on a non-standard RFCOMM channel (the default is channel <code>1</code>), you can specify it manually:</p> <p>From Windows: <pre><code>python bsh_client_windows.py user@00:11:22:33:44:55 -p 3\n</code></pre></p> <p>From Linux: <pre><code>python3 bsh_client_linux.py user@00:11:22:33:44:55 -p 3\n</code></pre></p> <p>[!TIP] The OpenBSH client provides a highly responsive PTY experience. If connecting to a Linux server, the client will automatically disable local terminal echo, relying entirely on the server's PTY to render keystrokes, ensuring that programs like <code>vim</code>, <code>nano</code>, and <code>htop</code> work flawlessly.</p>"},{"location":"setup/linux-server/","title":"Linux Server Setup","text":"<p>OpenBSH runs as a native systemd service on Linux, providing seamless, unprivileged SSH-style access over Bluetooth.</p>"},{"location":"setup/linux-server/#prerequisites","title":"Prerequisites","text":"<p>Before installing the OpenBSH server, ensure that Bluetooth is enabled on your system and the necessary development libraries are installed.</p>"},{"location":"setup/linux-server/#install-dependencies","title":"Install Dependencies","text":"<p>=== \"Debian / Ubuntu\"     <pre><code>sudo apt update\nsudo apt install python3 python3-pip libbluetooth-dev libpam0g-dev bluez\n</code></pre></p> <p>=== \"Fedora / RHEL\"     <pre><code>sudo dnf install python3 python3-pip bluez-libs-devel pam-devel bluez\n</code></pre></p> <p>=== \"Arch Linux\"     <pre><code>sudo pacman -S python python-pip bluez bluez-libs bluez-utils\n</code></pre></p>"},{"location":"setup/linux-server/#verify-bluetooth-service","title":"Verify Bluetooth Service","text":"<p>Make sure BlueZ is running: <pre><code>sudo systemctl enable --now bluetooth\nbluetoothctl show          # verify adapter is detected\n</code></pre></p>"},{"location":"setup/linux-server/#installation","title":"Installation","text":"<p>The Linux port includes a unified install script that handles copying files, creating directories, and installing the systemd unit.</p> <ol> <li>Clone or download the OpenBSH repository.</li> <li>Navigate to the <code>linux/</code> directory.</li> <li>Run the installer script:</li> </ol> <pre><code>sudo bash install.sh\n</code></pre>"},{"location":"setup/linux-server/#what-the-script-does","title":"What the script does:","text":"<ul> <li>Installs Python dependencies (<code>cryptography</code>, <code>python-pam</code>, <code>PyBluez</code>) globally.</li> <li>Copies the core BSH python files to <code>/opt/bsh/</code>.</li> <li>Creates necessary state directories: <code>/var/lib/bsh/</code>, <code>/var/log/bsh/</code>, <code>/run/bsh/</code>, <code>/etc/bsh/</code>.</li> <li>Generates a default configuration file at <code>/etc/bsh/config.json</code>.</li> <li>Installs and enables the <code>bsh.service</code> systemd unit.</li> </ul> <p>[!NOTE] OpenBSH must run as root (UID 0) to properly hook into PAM and to impersonate the logged-in user via <code>setuid</code> / <code>setgid</code>.</p>"},{"location":"setup/linux-server/#configuration","title":"Configuration","text":"<p>The main configuration file is located at <code>/etc/bsh/config.json</code>. </p> <pre><code>{\n  \"channel\":       1,\n  \"log_level\":     \"DEBUG\",\n  \"password_file\": \"/var/lib/bsh/passwords\",\n  \"log_file\":      \"/var/log/bsh/bsh_service.log\"\n}\n</code></pre> <p>If you modify this file, you must restart the service: <pre><code>sudo systemctl restart bsh\n</code></pre></p>"},{"location":"setup/linux-server/#service-management","title":"Service Management","text":"<p>OpenBSH on Linux relies on <code>systemd</code> for lifecycle management, though you can use the built-in python script as well.</p>"},{"location":"setup/linux-server/#using-systemd-recommended","title":"Using Systemd (Recommended)","text":"<pre><code>sudo systemctl start   bsh\nsudo systemctl stop    bsh\nsudo systemctl restart bsh\nsystemctl status       bsh\n</code></pre>"},{"location":"setup/linux-server/#viewing-logs","title":"Viewing Logs","text":"<p>Logs are managed by both systemd's journal and a local log file. <pre><code># View systemd journal\njournalctl -u bsh -f\n\n# View file logs\ntail -f /var/log/bsh/bsh_service.log\n</code></pre></p>"},{"location":"setup/linux-server/#user-management","title":"User Management","text":"<p>By default, OpenBSH will attempt to authenticate against the native Linux PAM module (or fallback to <code>/etc/shadow</code>). However, you can manage standalone BSH users using the built-in password database.</p> <pre><code># Add a standalone BSH user\nsudo python3 /opt/bsh/bsh_password.py adduser alice\n\n# Add a BSH user mapped to a specific system user\nsudo python3 /opt/bsh/bsh_password.py adduser bsh_alice --system-user alice\n\n# List users\nsudo python3 /opt/bsh/bsh_password.py list\n\n# Change password\nsudo python3 /opt/bsh/bsh_password.py passwd alice\n\n# Delete user\nsudo python3 /opt/bsh/bsh_password.py deluser alice\n</code></pre> <p>[!IMPORTANT] When adding a standalone user, the username must either perfectly match an existing system user (e.g., <code>alice</code>), OR you must explicitly map it to an existing system user using <code>--system-user</code>. If the mapping does not exist, the shell will fail to spawn.</p>"},{"location":"setup/windows-server/","title":"Windows Server Setup","text":"<p>The OpenBSH Server on Windows is designed to run seamlessly as a background Windows Service. It hooks directly into the Windows security model to authenticate users and spawn restricted shell sessions.</p>"},{"location":"setup/windows-server/#prerequisites","title":"Prerequisites","text":"<p>Before installing the OpenBSH server, ensure that Bluetooth is turned on and discoverable on your Windows machine.</p>"},{"location":"setup/windows-server/#install-python","title":"Install Python","text":"<p>Ensure Python 3.8 or higher is installed and added to your system <code>PATH</code>.</p>"},{"location":"setup/windows-server/#install-dependencies","title":"Install Dependencies","text":"<p>Open a Command Prompt or PowerShell terminal as Administrator and install the required Python packages. OpenBSH heavily relies on <code>pywin32</code> for service and system integration.</p> <pre><code>pip install pywin32 cryptography\n</code></pre>"},{"location":"setup/windows-server/#installation","title":"Installation","text":"<p>The Windows server relies on <code>win32serviceutil</code> to register itself as a native Windows service.</p> <ol> <li>Download or clone the OpenBSH repository.</li> <li>Navigate to the <code>windows/</code> directory.</li> <li>Open an Administrator command prompt.</li> <li>Run the installation command:</li> </ol> <pre><code>python bsh_service.py install\n</code></pre>"},{"location":"setup/windows-server/#what-the-installation-does","title":"What the installation does:","text":"<ul> <li>Registers the python script as a Windows Service named <code>BSH Service</code> (Bluetooth Shell Service).</li> <li>Sets the service to start automatically on boot.</li> <li>The service will run under the <code>LocalSystem</code> account, which is required to impersonate logged-in users via <code>CreateProcessAsUser</code>.</li> </ul> <p>[!WARNING] Do NOT change the service user to a standard domain or local user account. The <code>LocalSystem</code> context holds the <code>SE_TCB_NAME</code> and <code>SE_ASSIGNPRIMARYTOKEN_NAME</code> privileges necessary for shell impersonation.</p>"},{"location":"setup/windows-server/#service-management","title":"Service Management","text":"<p>You can manage the service using the provided Python script, the Windows Services MMC snap-in (<code>services.msc</code>), or via standard Windows command-line tools.</p>"},{"location":"setup/windows-server/#using-python-command-line","title":"Using Python Command Line","text":"<pre><code># Start the service\npython bsh_service.py start\n\n# Stop the service\npython bsh_service.py stop\n\n# Restart the service\npython bsh_service.py restart\n</code></pre>"},{"location":"setup/windows-server/#using-windows-built-ins","title":"Using Windows Built-ins","text":"<pre><code># Start\nnet start BSHService\n\n# Stop\nnet stop BSHService\n</code></pre>"},{"location":"setup/windows-server/#user-authentication","title":"User Authentication","text":"<p>On Windows, OpenBSH uses <code>LogonUserW</code> to validate the supplied credentials for the target Windows account.</p> <p>You do not need to create standalone BSH users. Any user who can log into the Windows machine locally can authenticate over BSH.</p> <p>To connect from a client, simply use your Windows username and password.</p> <p>In practice, the current client flow is simplest when the username matches the Windows account name expected by the server configuration.</p>"},{"location":"setup/windows-server/#the-standalone-bsh-password-db-optional","title":"The Standalone BSH Password DB (Optional)","text":"<p>If you wish to use passwords independent of the Windows system, you can use the built-in password database. Similar to the Linux version, these users will still need to be mapped to a legitimate Windows system user.</p> <pre><code>python bsh_password.py adduser standalone_bsh_user\n</code></pre>"},{"location":"setup/windows-server/#file-locations-logging","title":"File Locations &amp; Logging","text":"<ul> <li>Logs Location: <code>C:\\ProgramData\\BSH\\logs\\</code></li> <li>Configuration: The default configuration file is <code>C:\\ProgramData\\BSH\\config.json</code>, and runtime state is written under <code>C:\\ProgramData\\BSH</code>.</li> </ul> <p>If you experience connection failures, always check the latest log file in <code>C:\\ProgramData\\BSH\\logs\\</code>.</p>"}]}